"""P0 — the seed phase.

Human-authored business prose is faithfully rewritten into a template-conformant seed.
This package owns the template definition, the markdown reader, and the structural oracle
that decides seed admissibility. It reads no snapshot.
"""
